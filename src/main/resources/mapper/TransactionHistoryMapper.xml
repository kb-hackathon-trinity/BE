<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.trinity.be.transactionhistory.mapper.TransactionHistoryMapper">

    <select id="get" resultType="transactionHistoryDTO">
        SELECT *
        FROM transaction_history
    </select>

</mapper>